<UserControl x:Class="Cache_Memory_Benchmark.MVVM.View.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cache_Memory_Benchmark.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="560" d:DesignWidth="800" >
    <StackPanel Margin="5">
        <StackPanel Height="50">
            <TextBlock Text="{DynamicResource History}"
                       FontWeight="Bold"
                       FontSize="26"
                       Foreground="White"
                       Margin="10"/>
        </StackPanel>
        <StackPanel>
            <ScrollViewer Height="500">
                <Grid>
                    <TextBlock Name="History_tb"
                        Text=""
                       Foreground="White"
                       Margin="10"/>
                </Grid>
            </ScrollViewer>
        </StackPanel>

    </StackPanel>

</UserControl>
